def accuracy(y_true: list[int], y_pred: list[int]) -> float:
    """Accuracy, rounded to 4 dp."""
    # TODO
    return 0.0

def precision(y_true: list[int], y_pred: list[int]) -> float:
    """Precision (0.0 if no positive predictions), rounded to 4 dp."""
    # TODO
    return 0.0

def recall(y_true: list[int], y_pred: list[int]) -> float:
    """Recall (0.0 if no true positives exist), rounded to 4 dp."""
    # TODO
    return 0.0

def f1_score(y_true: list[int], y_pred: list[int]) -> float:
    """F1 score (0.0 if precision+recall=0), rounded to 4 dp."""
    # TODO
    return 0.0
