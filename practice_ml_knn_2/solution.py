import math

def knn_predict(X_train: list[list[float]], y_train: list[float],
                x_query: list[float], k: int) -> float:
    """k-NN regression: return mean of k nearest targets, rounded to 4 dp."""
    # TODO
    return 0.0
