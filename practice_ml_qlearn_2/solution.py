import random

def epsilon_greedy(q_values: list[float], epsilon: float, seed: int) -> int:
    """Epsilon-greedy action selection. See problem statement for exact procedure."""
    # TODO
    return 0
